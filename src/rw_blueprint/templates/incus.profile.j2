# Generated by rw_blueprint. Do not edit by hand.
# Source of truth: topology.yaml (node: {{ node.id }})

name: {{ node.id }}
description: {{ node.id }} ({{ node.type }}){% if node.host %} hosted on {{ node.host }}{% endif %}
config:
{% if node.cpu %}  limits.cpu: "{{ node.cpu }}"
{% endif %}{% if node.memory %}  limits.memory: {{ node.memory }}
{% endif %}devices:
{% if node.nic %}  eth0:
    type: nic
    nictype: {{ node.nic }}
{% endif %}